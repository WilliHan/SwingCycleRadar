db_path: ./data/swingcycle.db
log_level: INFO

http:
  timeout_sec: 20
  max_retries: 3

backfill:
  min_lookback_days: 350

reports:
  retention_days: 90  # data/exports/{trade_date}/ 보존 기간 — 이보다 오래된 건 자동 삭제
